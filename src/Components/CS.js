import React from 'react'
import Base from './Base'

export default function CS() {
  return (
    <div>
    <Base/>
    <h1 id='cont'>Since, the aplication is not in working condition, the service providing team is not recruited.<br></br> So kindly stay tunned to get further bug fixes...</h1>
    </div>
  )
}
